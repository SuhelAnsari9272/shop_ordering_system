# Root requirements file for running the full system locally WITHOUT Docker.
# In production / Docker, each service uses its own requirements.txt
# (backend/requirements.txt, customer_app/requirements.txt, admin_app/requirements.txt).
#
# Install with: pip install -r requirements.txt

# --- Backend (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
python-dotenv==1.0.1

# --- Frontend (Streamlit apps) ---
streamlit==1.41.1
requests==2.32.3
